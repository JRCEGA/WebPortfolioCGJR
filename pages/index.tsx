// index.tsx
import Head from "next/head";
import Link from "next/link";
import Image from "next/image";
import localFont from "next/font/local";
import { FaGithub, FaLinkedin } from "react-icons/fa";
import { BsFillPersonLinesFill } from "react-icons/bs";
import { SpeedInsights } from "@vercel/speed-insights/next"
import { PiBracketsAngle } from "react-icons/pi";
import { MdPermContactCalendar } from "react-icons/md";
import { IoSchoolSharp } from "react-icons/io5";
import DarkModeToggle from "./DarkModeToggle";
import { useEffect, useState } from 'react';
import Header from "./Header";

const Home = () => {
  const [isLoaded, setIsLoaded] = useState(false);

  // Age calculation function
  const calculateAge = (birthDate: Date) => {
    const today = new Date();
    let age = today.getFullYear() - birthDate.getFullYear();
    const monthDiff = today.getMonth() - birthDate.getMonth();
    
    if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birthDate.getDate())) {
      age--;
    }
    
    return age;
  };

  const birthDate = new Date(2003, 0, 8);
  const age = calculateAge(birthDate);

  // useEffect executes when the component is mounted, activating the animation
  useEffect(() => {
    setIsLoaded(true); // Activates the animations when the component loads
  }, []);

  return (
    <>
      <Head>
        <title>Jose R Cebreros | CS Graduate</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/jr-favicon.ico" />
      </Head> 
      <div className="UIMainScreen">
        <Header />
        <main>
          <div className="mainScreen">
            <div className={`SelfPortrait ${isLoaded ? 'fade-in-scale delay-1' : ''}`}>
              <img
                src="/me.png"
                alt="Portrait of myself."
                className="responsive-image"
              />
            </div>
            <div className="IntroText">
              <span className={`${isLoaded ? 'fade-in-up delay-2' : ''}`}>Hello, Welcome</span>
              <p className={`${isLoaded ? 'fade-in-up delay-3' : ''}`}>
                I'm <b>Jose Roberto Cebreros</b>, a {age}-year-old recent Computer Science graduate from CETYS Universidad in Tijuana, Mexico.
              </p>
              <p className={`${isLoaded ? 'fade-in-up delay-3' : ''}`}>
                I'm currently holding a trainee position as a Software QA Engineer at <a href="https://www.linkedin.com/company/dexcom/" target="_blank" rel="noopener noreferrer">Dexcom Inc</a>, in San Diego, California.
              </p>
              <br />
              <p className={`${isLoaded ? 'fade-in-up delay-3' : ''}`}>I encourage you to follow me on my social, and learn more About me:</p>
              <br />
              <div className={`Buttons ${isLoaded ? 'fade-in-up delay-4' : ''}`}>
                <Link href="/AboutMe">
                  <button className="ButtonsContent">About Me <BsFillPersonLinesFill /></button>
                </Link>
                <Link href="/WorkEducation">
                  <button className="ButtonsContent">Work/Education <IoSchoolSharp /></button>
                </Link>
                <Link href="/Projects">
                  <button className="ButtonsContent">Projects <PiBracketsAngle /></button>
                </Link>
                <Link href="/ContactInfo">
                  <button className="ButtonsContent">Contact Me <MdPermContactCalendar /></button>
                </Link>
              </div>
            </div>
          </div>
        </main>
      </div>
    </>
  );
};

export default Home;
